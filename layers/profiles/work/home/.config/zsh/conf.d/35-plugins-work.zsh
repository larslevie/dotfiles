# snippet_if comes from the common 30-plugins.zsh, which sorts before this.
# These load in turbo too — see the note there on startup cost.
snippet_if aws     OMZP::aws
snippet_if kubectl OMZP::kubectl
snippet_if kubectx OMZP::kubectx
