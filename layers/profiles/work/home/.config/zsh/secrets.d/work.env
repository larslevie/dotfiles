# Pointers only — no secret values. Resolved by `opload` (conf.d/80-secrets).
# Referenced by item ID, not title: the vault holds duplicate titles.

# Grafana Witco API Token
GRAFANA_URL=https://grafana.witco.net
GRAFANA_TOKEN=op://Witco/kwcoxvjq6t7slaeaoy7ye2ccja/password
