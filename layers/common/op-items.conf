# SSH public keys pulled from 1Password by `dot keys`. Not stowed — this
# lives outside home/ so stow never touches it.
# <path relative to $HOME>  <op:// reference>
.ssh/git_signing.pub      op://Private/Git Signing Key/public key
.ssh/github_personal.pub  op://Private/GitHub - Personal/public key
