# Which profile each machine gets. One "<hostname> <profile>" pair per line.
# Hostname is `scutil --get LocalHostName` (falls back to `hostname -s`).
# A machine may also have an optional layers/hosts/<hostname>/ overlay.

K23DRYHMHQ	work
